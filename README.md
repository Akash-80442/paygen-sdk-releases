# PayGen SDK Releases
You can download the raw AAR here, or use it via Gradle:
```kotlin
repositories {
    maven { url = uri("https://raw.githubusercontent.com/Akash-80442/paygen-sdk-releases/maven-repo") }
}
dependencies {
    implementation("com.paygen.sdk:paygen-sdk:1.1.2")
}
```
